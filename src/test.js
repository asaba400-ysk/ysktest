const i = 1;
const s = 0;

const m = i || s;




const retu4 = (x)=>{
    return x;
}

const ar = [1,2,3];

ar.map(x => x * 2);

/*const test = (func)=>{//こうかい関数
    console.log(1);
    func(5);
}

const call = (x)=>{ //コールバック関数
    console.log(x);
}
test(call);*/




/*
const test = (func)=>{//こうかい関数
    console.log(1);
    func(5);
}

test((x)=>{ //コールバック関数
    console.log(x);
});*/

/*const test = (func)=>{//こうかい関数
    console.log(1);
    func();
}

test(()=>{//こうかい関数
    console.log(2)
});*/

const num = [1,2,3,4,5];

/*const res = num.map((a,b,c)=>{
    console.log("返します");
    return a + 1;
 });*/

const state = {
    history:[{
        squares: Array(9).fill(null),
    }]
};

const num =state.history.concat()
console.log(res);

